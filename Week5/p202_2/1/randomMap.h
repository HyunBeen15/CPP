#include <stdio.h>                // 입출력 
#include <stdlib.h>               // rand(), srand() 사용하기 위해 필요
#include <time.h>                 // time(NULL) 사용하기 위해 필요, 매번 다른 난수

void randomMap(int map[5][5]);    // 함수 선언

